<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (c) 2026 keese-ai -->

---
scope: plan
category: register
depends:
  - README.md
  - D1-controller-wiring.md
  - D2-runtime-spi-minimum.md
  - D3-cluster-bootstrap.md
  - D4-cloud-deploy.md
  - D5-demo-smoke.md
related_skills: [plan-management]
status: current
last_verified: 2026-05-07
---

# Tech-debt register — post-demo cleanup

> Captures every shortcut taken in the demo track (D1–D5) and every
> stub flagged in the 2026-04-25 readiness audit. Severity tiers:
>
> - **P1** — ship within one week of demo. Blocks any second user, any
>   second tenant, any production-grade incident handling.
> - **P2** — ship within one month. Blocks v0.1.0 GA channel.
> - **P3** — ship within one quarter. Blocks v0.2.0+ feature scope.

## P1 — within one week

| ID | Item | Source | Spec / Design |
|---|---|---|---|
| ~~TD-P1-01~~ | ~~Replace `FakeRebacWriter` with real OpenFGA SDK...~~ Closed 2026-05-07: `github.com/openfga/go-sdk` was already in `go.mod`; the gap was that every controller's `SetupWithManager` nil-guard instantiated a per-package `*FakeRebacWriter` instead of the real `*OpenFGARebacWriter`. Fix: (a) moved all `*FakeRebacWriter` struct definitions from production `*_rebac.go` files into `*_rebac_fake_test.go` files (same package, test-only build); (b) introduced per-package `*NoopRebacWriter` value types to replace the nil-guards in `SetupWithManager` (dev/local-no-OpenFGA still boots, emitting `"OPENFGA_API_URL unset — using noop writers"` via `setupLog.Info`); (c) `cmd/main.go` was already wiring `*OpenFGARebacWriter` when all three `OPENFGA_*` env vars are set — that path is now the only production path. `grep -rln FakeRebacWriter internal/ \| grep -v _test.go` returns zero. `go build ./...` + `go test -short -race ./...` (unit tier) green. 11 `*_fake_test.go` files created; 19 production files modified (9 `*_rebac.go`, 8 `*_controller.go`, `cmd/main.go`, `internal/rebac/client.go` + `doc.go`). | audit 2026-04-25; deferred from D1 | [docs/specs/egress-authz-protocol.md](../../specs/egress-authz-protocol.md) |
| ~~TD-P1-02~~ | ~~Implement `AgentRuntime` SPI...~~ Closed 2026-05-06: full SPI surface at [internal/runtime/spi/v1alpha1/](../../../internal/runtime/spi/v1alpha1/) with `AgentRuntime` interface (`Bootstrap`, `Run`, `Resume`, `Drain`, `Attach`, `CleanupSubAgents`, `InjectPrompt`, `InvokeSubAgent`, `Health`, `StreamEvents`) + sentinel errors (`ErrBudget`, `ErrAttachUnsupported`, `ErrSubAgentLimitExceeded`). Goose provider at [internal/runtime/providers/goose/](../../../internal/runtime/providers/goose/) implements Bootstrap/Run/Resume/Drain; advanced methods gate behind capability flags. New preStop sidecar binary [cmd/keese-drain/main.go](../../../cmd/keese-drain/main.go) installs SIGTERM handler, writes JSON checkpoint marker atomically to `/var/run/keese/session/sessions/<uid>/draining` before pod termination. WorkspaceSession controller's preStop replaced (was `kill -TERM 1 && sleep 25`). 9-file kuttl suite at [tests/e2e/agentruntime-drain/](../../../tests/e2e/agentruntime-drain/) proves the Drain marker + Resume round-trip across pod replacement on a shared PVC. **Follow-ons:** (a) `Attach`/`InjectPrompt`/`InvokeSubAgent` are stubs returning `ErrAttachUnsupported`/no-op/`ErrSubAgentLimitExceeded`; full ACP bridge wiring (08b) and real sub-agent spawn deferred to TD-P3-04 + TD-P3-05; (b) bake `keese-drain` into the published goose image (currently the kuttl test uses busybox because no published image carries the binary) — tracked under TD-P3-05. | D2-T5 | [docs/designs/07-agent-runtime-spi.md](../../designs/07-agent-runtime-spi.md), [docs/specs/agent-runtime-spi.md](../../specs/agent-runtime-spi.md) |
| ~~TD-P1-03~~ | ~~Wire ext_authz on Envoy AI Gateway against OpenFGA.~~ Closed 2026-05-06: keese-authz Envoy ext_authz gRPC server at [cmd/keese-authz/](../../../cmd/keese-authz/) + [internal/authz/extauth/](../../../internal/authz/extauth/). Two new authz CRDs ([ToolBinding](../../../api/authz/v1alpha1/toolbinding_types.go) + [WorkspaceTool](../../../api/authz/v1alpha1/workspacetool_types.go)) compile into an in-memory routing trie via controller-runtime polling watcher (10s interval). `Workspace.spec.egress.allowedTools[]` writes `tool:<n>#allowed_in@workspace:<n>` ReBAC tuples. Bootstrap manifest [dev/bootstrap/aigateway/keese-authz.yaml](../../../dev/bootstrap/aigateway/keese-authz.yaml) wires `gateway.envoyproxy.io/v1alpha1.SecurityPolicy.spec.extAuth.grpc` to the service. Verified end-to-end on demo cluster: with tuple present → HTTP 200 + audit log `decision: allow, duration_ms: 18`; without tuple → HTTP 403 `permission_denied` + audit log `decision: deny, reason: openfga_denied`. **Follow-ons:** (a) configure `with_request_body` so bodyDiscriminator fires (today only the bare `tool:anthropic.messages` resolves, not sub-tool variants like `.opus-4`); (b) replace the trie-refresher's polling with informer event handlers; (c) bake the openfga-config CM mirror into the seed Job (currently manual `kubectl get cm -n openfga \| sed \| apply`). | D3-T2 | [docs/designs/22-egress-toolbinding.md](../../designs/22-egress-toolbinding.md), [docs/designs/05a-envoy-ai-gateway-topology.md](../../designs/05a-envoy-ai-gateway-topology.md) |
| ~~TD-P1-04~~ | ~~Pre-install ValidatingWebhook for cosign on InstallPlans (designed in 14a F7, never implemented).~~ Closed 2026-05-06: `keese-cosign-webhook` lands at [cmd/keese-cosign-webhook/](../../../cmd/keese-cosign-webhook/) + [internal/admission/cosign/](../../../internal/admission/cosign/) + [config/cosign-webhook/](../../../config/cosign-webhook/). Validates `operators.coreos.com/v1alpha1.InstallPlan` on CREATE+UPDATE, walks each referenced CSV's `spec.relatedImages` + deployment containers, and shells out to cosign with the rule-05.12 identity regexp + GitHub OIDC issuer for any image under `ghcr.io/keese-ai/`. `failurePolicy: Fail`, 2 replicas, PDB `minAvailable: 1`, cert-manager-issued TLS, fail-closed network policy with explicit Sigstore + ghcr egress. Break-glass via the rule-05.13 namespace label + InstallPlan annotation pair. Image build + cosign sign added to [.github/workflows/image.yaml](../../../.github/workflows/image.yaml) as a matrix entry. **Update 2026-05-06:** retrofitted onto the [D27](../../designs/27-feature-gates-openfeature.md) feature-gate plumbing — handler gates on `cosign-installplan-verify` + `cosign-installplan-failclosed`; both ship as alpha-default-off seed CRs at [config/featuregates/](../../../config/featuregates/), so the webhook is shipped-but-quiet by default (operators flip the override on per-cluster). **Follow-ons:** (a) digest-pin the cosign builder stage in [Dockerfile.keese-cosign-webhook](../../../Dockerfile.keese-cosign-webhook) (currently floating `v2.4.1` tag pending TD-P2-11's `pin-cosign-digest.sh`); (b) author the cosign-tamper kuttl test under `test/e2e/bundle-sign/` (design 14a-ii spec); (c) wire a kustomize patch for air-gapped overlays to point at an internal Sigstore mirror. | D4-T2 fallback | [docs/designs/14a-olm-channels-upgrades.md](../../designs/14a-olm-channels-upgrades.md), [docs/designs/27-feature-gates-openfeature.md](../../designs/27-feature-gates-openfeature.md), [docs/references/csv-rotate-to-signed-bundle.md](../../references/csv-rotate-to-signed-bundle.md) |
| ~~TD-FEATURE-GATES~~ | Feature-gate plumbing per [D27](../../designs/27-feature-gates-openfeature.md). Closed 2026-05-06: new CRD `policy.keese.ai/v1alpha1.FeatureGate` ([api/policy/v1alpha1/featuregate_types.go](../../../api/policy/v1alpha1/featuregate_types.go) + CRD manifest), `FeatureGateController` ([internal/controller/policy/featuregate_controller.go](../../../internal/controller/policy/featuregate_controller.go)) projects effective values into ConfigMap `keese-system/keese-features`, `internal/featuregate` package wraps OpenFeature SDK 1.17.2 with an in-process provider over `atomic.Value[map[string]bool]` + fsnotify reload (debounced 500 ms). Tamper resistance via kyverno ClusterPolicy at [config/featuregates/policy.yaml](../../../config/featuregates/policy.yaml). Tooling: `make featuregate-list` + `make featuregate-diff`. Catalog at [27b](../../designs/27b-feature-gate-catalog.md). Cosign-webhook is the first consumer (TD-P1-04 update above). | TD-P1-04 retrofit | [docs/plans/td-feature-gates-openfeature.md](../td-feature-gates-openfeature.md), [docs/designs/27-feature-gates-openfeature.md](../../designs/27-feature-gates-openfeature.md) |
| ~~TD-P1-05~~ | ~~If D4-T2 used the local-build fallback, push a CI-built signed image and update the running CSV by approving a new InstallPlan. Bundle currently shipped without cosign attestation in that path.~~ Closed 2026-05-06: [scripts/bundle-sign-verify.sh](../../../scripts/bundle-sign-verify.sh) + `make bundle-sign-verify` target wrap `cosign verify` keyless against the rule-05.12 identity. The `if [ -f Dockerfile ]` / `if [ -d bundle ]` skip clauses in [.github/workflows/image.yaml](../../../.github/workflows/image.yaml) + [.github/workflows/bundle.yaml](../../../.github/workflows/bundle.yaml) were removed; missing inputs now hard-fail CI. Both workflows now run `bundle-sign-verify.sh` as a self-check after `cosign sign` so an upload that didn't actually sign blocks the green build. D4-T2 docs replaced the local-build fallback paragraph with a pointer to [docs/references/csv-rotate-to-signed-bundle.md](../../references/csv-rotate-to-signed-bundle.md). `make docker-push` + `make cosign-webhook-push` now print a `WARN: local push is unsigned — production must use image.yaml on tag push` reminder. **Follow-ons:** (a) wire `make bundle-sign-verify` as a required GitHub branch protection check on tags; (b) audit production overlays for any digest-less image references (rolls into TD-P2-09). | D4-T2 fallback | rule 05.12, [docs/references/csv-rotate-to-signed-bundle.md](../../references/csv-rotate-to-signed-bundle.md) |
| ~~TD-P1-06~~ | ~~Workspace controller predicate that filters on `keese.ai/managed: "true"`...~~ Closed 2026-05-06: ADR [docs/designs/26-workspace-managed-predicate-adr.md](../../designs/26-workspace-managed-predicate-adr.md) commits to predicate-free reconcile across every keese kind. Inline rationale: keese owns its own API groups (no second consumer), label-stamping every sample is a footgun, RBAC is the proper tenant-scoping primitive, and the existing break-glass annotation (rule 05.13) is the right escape hatch. Suspended-state semantics — if needed later — go in spec, not a label predicate. | D1-T2 | [docs/designs/26-workspace-managed-predicate-adr.md](../../designs/26-workspace-managed-predicate-adr.md) |
| ~~TD-P1-07~~ | ~~Author live `tests/e2e/kuttl-config.yaml` and kuttl test cases.~~ Closed 2026-05-06: kuttl progression case at [tests/e2e/workspace-progression/](../../../tests/e2e/workspace-progression/) asserts `Tenant=Active`, `Workspace=Ready`, `WorkspaceSession=Active` via `kubectl wait` (not native kuttl resource matching — the latter's exact slice-length matcher is too strict for status conditions). `kuttl` now in [flake.nix](../../../flake.nix) (binary is `kubectl-kuttl`; nixpkgs attr is `kuttl`). `make test-e2e` runs both cases. **Note:** the `aigw-defense` case currently fails because of a separate regression that surfaced during this work — the group-rename agent's controller merge dropped the gateway-CA ConfigMap + recipe-CM volume mounts from the session pod's `sessionPodVolumes`. Tracked as TD-P1-07-followon. | D5 §verification | [docs/references/envtest-kuttl-harness.md](../../references/envtest-kuttl-harness.md) |
| ~~TD-P1-07-followon~~ | ~~Restore gateway-CA + recipe ConfigMap volume mounts...~~ Closed 2026-05-06: extracted `sessionPodVolumes` + `sessionPodVolumeMounts` + `sessionPodEnv` helpers in [workspacesession_controller.go](../../../internal/controller/keese/workspacesession_controller.go); always-on session/sa-token/scratch/gateway-ca + conditional recipe ConfigMap mount when `Workspace.spec.recipeRef` is set. Env now includes `SSL_CERT_FILE=/var/run/keese/ca/ca.crt`, `ANTHROPIC_BASE_URL`/`ANTHROPIC_HOST` path-prefixed to `/anthropic`, and `KEESE_RECIPE_PATH` when a recipe is mounted. The aigw-defense kuttl test was also updated for the post-TD-P1-03 architecture (ext_authz now sits in front of the gateway and treats `Authorization: Bearer <SA-token>` as the canonical identity claim — old test's "garbage Bearer gets stripped+replaced" semantic is gone). New test surface: `no-auth → 403`, `garbage Bearer → 403`, `valid SA token + hostile x-api-key → 200`, etc. All 5 cases pass. | 2026-05-06 rename regression | n/a |
| ~~TD-P1-08~~ | ~~Helmfile chart versions: every chart in helmfile.yaml is `# unverified-2026`...~~ Closed 2026-05-06: 8 charts bumped to current latest (argo-workflows 0.42.5→0.47.5, eck-operator 2.13.0→3.4.0, kyverno 3.2.6→3.8.0, nack 0.26.0→0.33.2, nats 1.2.6→1.3.16, openbao 0.4.0→0.9.0, openfga 0.2.27→0.2.62, qdrant 1.13.0→1.17.1). 4 charts held with inline rationale (capsule 0.7.2 — 0.8/0.9 break Tenant.spec; external-secrets 0.10.5 — see new TD-P2-18; envoy-gateway v1.6.0 + envoy-ai-gateway v0.5.0 — pinned per commit 98e6550 ext_proc validation). helmfile.lock regenerated. | D3-T1 | n/a |
| ~~TD-P1-09~~ | ~~sqlite WAL + RWO PVC + restart can corrupt the DB mid-write...~~ Closed 2026-05-06: single-pod-per-Memory invariant documented in [docs/specs/keese.ai-v1alpha1-memory.md](../../specs/keese.ai-v1alpha1-memory.md) §"sqlite single-pod-per-Memory invariant". Three controller-side enforcements (RWO PVC, per-Memory UID-named PVC, single session pod per (Workspace, AttachSubject)) plus production guidance to switch to a network-attached provider for multi-replica. Admission-time enforcement via VAP `SqliteSingleConsumer` deferred to TD-P2-08. | D2-T2 | [docs/specs/keese.ai-v1alpha1-memory.md](../../specs/keese.ai-v1alpha1-memory.md), [docs/designs/15-memory-management.md](../../designs/15-memory-management.md) |
| ~~TD-P1-10~~ | ~~Helm OCI charts don't upgrade CRDs across release bumps...~~ Closed 2026-05-06: [dev/bootstrap/install-crds.sh](../../../dev/bootstrap/install-crds.sh) pulls each chart's `crds/*.yaml` and applies via `kubectl apply --server-side --force-conflicts` BEFORE helmfile sync. Wired into `make bootstrap-infra`. Currently lists envoy-gateway only (the chart that bit us); add new entries to `CHARTS_NEEDING_PREAPPLY` when adding new charts that ship CRDs via Helm's `crds/` convention. | D3-T1; 2026-04-29 EG v1.6 debug | n/a |
| ~~TD-P1-11~~ | ~~WorkspaceSession reconciler watches: Owns(Pod) + Owns(PVC) + poke-friendly predicate.~~ Closed 2026-05-06: [internal/controller/keese/workspacesession_controller.go](../../../internal/controller/keese/workspacesession_controller.go) `SetupWithManager` now calls `Owns(&corev1.Pod{})` + `Owns(&corev1.PersistentVolumeClaim{})` so the controller requeues on owned-resource deletion (recovers from `kubectl delete pod`, node drain, OOM, manual PVC cleanup). The `For()` predicate is now `Or(GenerationChangedPredicate, pokeAnnotationPredicate)` — annotations matching `keese.ai/poke*` trigger reconcile without bumping spec generation. Verified during TD-P1-02 demo (no more "delete + reapply session" recovery dance). | 2026-05-05 demo session | [internal/controller/keese/workspacesession_controller.go#L416](../../../internal/controller/keese/workspacesession_controller.go#L416) |

## P2 — within one month

| ID | Item | Source | Spec / Design |
|---|---|---|---|
| ~~TD-P2-01~~ | ~~WorkspaceShare ReferenceGrant projection is a stub at workspaceshare_controller.go:88-109.~~ Closed 2026-05-07: `sigs.k8s.io/gateway-api/apis/v1beta1` registered in `cmd/main.go` `init()` via `gatewayv1beta1.AddToScheme`. `buildReferenceGrant` constructs a `gatewayv1beta1.ReferenceGrant` that lives in the **source (workspace) namespace** per GW-API §5.1 — a grant must reside in the same namespace as its referent, not in the consumer namespace. SSA applied with `client.FieldOwner("keese-workspaceshare-controller")` + `client.ForceOwnership`. Deletion path calls `client.Delete` on the same grant object. New `ReferenceGrantProjected` condition on status and two new event reason constants (`ReasonShareReferenceGrantProjected`, `ReasonShareReferenceGrantPruned`) in `workspace_events.go`. `Owns(&gatewayv1beta1.ReferenceGrant{})` added to `SetupWithManager` watch. 7 pure-unit tests in `workspaceshare_referencegrant_test.go` cover name, namespace, From/To fields, TypeMeta, and labels. `go test -short -race ./internal/controller/keese/` green. | audit 2026-04-25 | [docs/specs/keese.ai-v1alpha1.md](../../specs/keese.ai-v1alpha1.md) |
| ~~TD-P2-02~~ | ~~Workflow trigger / output projections (CronJob, KEDA ScaledObject, Knative Trigger, HTTPRoute) are `TODO(spec-followup)` returns at workflow_controller.go:200-215.~~ Closed 2026-05-07: `reconcileTrigger` replaced with four real SSA helpers. `Cron` → `batch/v1.CronJob` (wf-launcher entrypoint, schedule/timezone/suspend forwarded, owner-ref set). `KnativeTrigger` → `eventing.knative.dev/v1.Trigger` (broker + CloudEvent attribute filter mapped, subscriber = `keese-wf-<name>-launcher` Service). `HTTPWebhook` → `gateway.networking.k8s.io/v1.HTTPRoute` (exact-path POST rule, backend = wf-launcher Service on 8080). `NATSSubscription` → sets `TriggerProjected=False/KEDAUnavailable` condition (no CRD projected; KEDA dep-conflict in go.mod). New `LauncherImage` field on `WorkflowReconciler` (defaults to `ghcr.io/keese-ai/keese:dev`). New status condition `TriggerProjected` with per-type reasons (`CronJobReady`, `TriggerReady`, `HTTPRouteReady`, `KEDAUnavailable`) in `workflow_events.go`. RBAC markers added for `eventing.knative.dev/triggers` and `gateway.networking.k8s.io/httproutes`. New sub-binary `cmd/keese-wf-launcher/main.go` with SIGTERM handler (rule 06.1) creates a `WorkflowRun` CR via in-cluster REST client. `hack/testdata/gateway-api/` + `hack/testdata/knative-eventing/` CRD fixtures for envtest; suite registers `batchv1`/`gatewayv1`/`eventingv1` schemes. 4 new integration test cases assert `TriggerProjected` condition reason per trigger type. Output projections (`reconcileOutput`) are a documented intentional no-op scoped to a separate follow-on. `go test -short -race ./internal/controller/keese/` green. 6 files changed + 1 new binary. | audit 2026-04-25 | [docs/specs/keese.ai-v1alpha1-workflow.md](../../specs/keese.ai-v1alpha1-workflow.md) |
| ~~TD-P2-03~~ | ~~RecipeSource git-clone path records a fake revision/digest.~~ Closed 2026-05-07: replaced stub in `reconcileGit` with real in-memory go-git clone (`github.com/go-git/go-git/v5` v5.19.0 + `go-billy/v5` v5.9.0 promoted to direct deps). New file [recipesource_git.go](../../../internal/controller/keese/recipesource_git.go) implements `GitCloner` interface via `DefaultGitCloner`: in-memory storer + billy `memfs.New()` (no disk writes — `readOnlyRootFilesystem: true`), branch/tag/SHA ref resolution via `repo.Reference()` + `repo.CommitObject()`, deterministic SHA-256 tree digest via sorted canonical tar. `loadGitAuth` reads `username`/`password`/`token` from a K8s Secret in the operator namespace (rule 05.7). Credential/URL redaction in all events and condition messages (rule 02). `RecipeSourceReconciler` gains `Cloner GitCloner` + `OperatorNamespace string` fields; `SetupWithManager` nil-guards to `DefaultGitCloner`. `CloneFailed` condition with redacted message on failure; `RequeueAfter: 5s`. New event reasons: `GitCloneFailed`, `GitRefNotFound`, `GitCloneSucceeded`. `FakeGitCloner` in [recipesource_git_fake.go](../../../internal/controller/keese/recipesource_git_fake.go). Two envtest cases added to [recipesource_controller_test.go](../../../internal/controller/keese/recipesource_controller_test.go): `GitCloneSuccess` (resolvedDigest + phase=Synced) and `GitCloneBadRef` (phase=Failed + CloneFailed condition). `go build ./internal/controller/keese/...` + `go test -short -race ./internal/controller/keese/...` green. 4 files changed (2 new), go.mod/go.sum updated. | audit 2026-04-25 | [docs/designs/16-recipe-distribution.md](../../designs/16-recipe-distribution.md) |
| ~~TD-P2-04~~ | ~~GuardrailBinding Envoy SecurityPolicy CEL evaluation is a stub at guardrailbinding_controller.go:226-234.~~ Closed 2026-05-07: new file [internal/controller/authz/guardrail_envoy.go](../../../internal/controller/authz/guardrail_envoy.go) implements `EnvoySecurityPolicyProjector` interface + `ClientSecurityPolicyProjector`. Production path: (1) builds a CEL expression `tool_name in [<allow>] && !(tool_name in [<deny>])` from the effective merged policy, (2) compiles + type-checks via `github.com/google/cel-go` (asserts bool output), (3) SSA-applies `gateway.envoyproxy.io/v1alpha1.SecurityPolicy` with `authorization.rules` encoding deny-wins header-match rules on `x-mcp-tool-name` (per TD-P1-03 ext_authz convention). CEL compile error → `CELCompilationFailed=True` condition + `Degraded` phase + `ReasonCELCompileError` event (rules 04.11, 02). `FakeEnvoyProjector` wired into suite; Tests 6+7 added: valid CEL → `Ready` + `Applied` recorded; injected `[CEL]` error → `Degraded` + `CELCompilationFailed=True`. `go test -short -race ./internal/controller/authz/...` green. Design ref: 06-guardrailbinding.md §Envoy SSA — Authorization.Rules header-match filter (no Lua). | audit 2026-04-25 | [docs/designs/06-guardrailbinding.md](../../designs/06-guardrailbinding.md) |
| ~~TD-P2-05~~ | ~~Transport NATS + cert-manager projections stub.~~ Closed 2026-05-07: (a) `transport_nats_nack.go` adds `ClientNatsStreamer` — SSA-projects `jetstream.nats.io/v1beta2.Stream` CRDs via unstructured Patch with `fieldOwner=keese-transport-controller`; subject naming follows design 03c (`keese.transport.<name>.>`); `AddStream`/`UpdateStream` share the same idempotent SSA Patch. (b) `transport_certmanager.go` gains `CertificateProjector` interface + `ClientCertificateProjector` (SSA-projects `cert-manager.io/v1.Certificate` with ECDSA-256, 90d/30d, `ClusterIssuer keese-<namespace>`) + `ClientCertManagerReader` (unstructured Get); `FakeCertificateProjector` for tests. (c) `TransportReconciler.CertProjector` field wired; `reconcileCertificate()` fires on annotation `keese.ai/auto-manage-cert=true`; `CertificateProjected` condition added. (d) RBAC: cert-manager `verbs` gains `create;update;patch`; jetstream gains `patch`. (e) Two new event reasons in `transport_events.go`: `CertificateProjected`, `CertificateProjectionFailed`, `NATSStreamProjected`. (f) `github.com/projectcapsule/capsule@v0.7.2` added to `go.mod` to fix pre-existing missing-module error; four stale imports in `tenant_controller.go` removed. `go test -short -race ./internal/controller/keese/...` green. | audit 2026-04-25 | [docs/specs/keese.ai-v1alpha1.md](../../specs/) |
| ~~TD-P2-06~~ | ~~Tenant controller Capsule namespace lookup stub at tenant_controller.go:284-340.~~ Closed 2026-05-07: `resolveModeBNamespaces` now performs a real `r.Get(ctx, client.ObjectKey{Name: capsuleName}, &capsuleTenant)` against `capsulev1beta2.Tenant` (cluster-scoped), projects `capsuleTenant.GetNamespaces()` into `Tenant.status.namespaces[]`, and sets the `CapsuleTenantResolved` condition (`True/CapsuleTenantFound` or `False/CapsuleTenantNotFound`) plus the `status.capsuleTenantResolved` bool in lock-step. On NotFound the reconciler requeues after 5 s with a warning event. `SetupWithManager` wires `Watches(&capsulev1beta2.Tenant{}, capsuleTenantMapper)` so Capsule Tenant namespace-list changes trigger re-aggregation of all referencing keese Tenants. RBAC marker `+kubebuilder:rbac:groups=capsule.clastix.io,resources=tenants,verbs=get;list;watch` added. `capsulev1beta2.AddToScheme` registered in `cmd/main.go`. Capsule Tenant CRD vendored to `hack/testdata/capsule/`; tenancy suite loads it and registers the Capsule scheme. Two new envtest cases: (1) Capsule Tenant exists → `status.namespaces` mirrors Capsule's list + `CapsuleTenantResolved=True`; (2) Capsule Tenant absent → `CapsuleTenantResolved=False` + `RequeueAfter=5s`. `go build ./cmd/main.go` + `go test -short -race ./internal/controller/keese/...` green. | audit 2026-04-25 | [docs/designs/01-tenancy-capsule.md](../../designs/01-tenancy-capsule.md) |
| ~~TD-P2-07~~ | ~~Validating + defaulting webhooks: none wired.~~ Closed 2026-05-07: [config/webhook/](../../../config/webhook/) lands with `service.yaml` + `manifests.yaml` (MutatingWebhookConfiguration + ValidatingWebhookConfiguration for `keese.ai/v1alpha1.Recipe`). [config/certmanager/](../../../config/certmanager/) adds a self-signed Issuer + Certificate CR (`serving-cert`) wired via `cert-manager.io/inject-ca-from` annotations. [config/default/manager_webhook_patch.yaml](../../../config/default/manager_webhook_patch.yaml) adds port 9443 + the `webhook-server-cert` volume mount to the manager Deployment. Go side: [internal/controller/keese/recipe_webhook.go](../../../internal/controller/keese/recipe_webhook.go) implements `RecipeWebhook` (satisfies both `webhook.CustomDefaulter` and `webhook.CustomValidator`); `SetupRecipeWebhookWithManager` registered in `cmd/main.go` immediately after `RecipeReconciler.SetupWithManager`. Defaulter sets `spec.model.provider="anthropic"` when absent; validator rejects empty `spec.tools[]`, empty `spec.model.modelID`, empty `spec.sourceRef.name`. The three-gate cross-resource check (tool/model/extension via GuardrailBinding) stays in the reconciler per rule 04.12. Unit tests in [recipe_webhook_test.go](../../../internal/controller/keese/recipe_webhook_test.go) cover all reject + accept paths. `go build ./...` + `go test -short -race ./internal/controller/keese/...` green; `kubectl --dry-run=client apply -f config/webhook/service.yaml -f config/webhook/manifests.yaml` clean. **cert-manager TLS:** wired via Certificate CR + CA injection annotation; requires cert-manager to be installed (same prerequisite as the cosign webhook from TD-P1-04). | audit 2026-04-25 | rule 04.12 |
| ~~TD-P2-08~~ | ~~VAP YAMLs (rule 04.12 says VAP first, webhook second).~~ Closed 2026-05-07: [config/vap/](../../../config/vap/) ships 4 `ValidatingAdmissionPolicy` + `ValidatingAdmissionPolicyBinding` pairs. (1) **embedding-dim-immutable** — rejects UPDATE where `spec.embeddingDim` changes on `Memory` or `SharedMemory`; `oldObject.spec.embeddingDim == object.spec.embeddingDim` CEL with zero-value guard. (2) **break-glass-annotation** — rejects `keese.ai/unsafe-*` annotations on any `keese.ai/*` or `authz.keese.ai/*` resource unless `namespaceObject.metadata.labels["keese.ai/break-glass"] == "true"` (rule 05.13). (3) **regional-sensitive** — rejects `GuardrailBinding` with `spec.scope.type=Tenant` when `namespaceObject.labels["keese.ai/region"]` doesn't match `namespaceObject.labels["keese.ai/cluster-region"]`; permit-all when either label is absent (design 05b bootstrap safety). (4) **sqlite-single-consumer** — rejects `Memory` CREATE/UPDATE with `spec.provider.type=sqlite` when any `replicas > 1` sub-field is set (closes TD-P1-09 admission-time follow-on). All four: `failurePolicy: Fail`, `validationActions: [Deny]`, SPDX headers, kustomize-friendly labels. `config/vap/kustomization.yaml` lists all four; referenced from `config/default/kustomization.yaml`. **CEL limitation:** `regional-sensitive` reads cluster region from `namespaceObject.labels["keese.ai/cluster-region"]` because VAP CEL cannot reach an arbitrary namespace (e.g. kube-system) — cluster-region label must be on the binding's own namespace; documented in the YAML comment. `kubectl --dry-run=client apply` on all 4 YAMLs clean. | audit 2026-04-25 | rule 04.12 |
| ~~TD-P2-09~~ | ~~config/overlays/prod with image digest pinning...~~ Closed 2026-05-06: [config/overlays/prod/](../../../config/overlays/prod/) lands with digest-pinned image (placeholder sha256:0000... until first CI publish; rule 05.12), resource requests/limits 200m+256Mi/1+1Gi, terminationGracePeriodSeconds=90 (TD-P2-17 alignment), zone topology spread constraints, [pdb.yaml](../../../config/overlays/prod/pdb.yaml) (minAvailable: 1), [resource-quota.yaml](../../../config/overlays/prod/resource-quota.yaml) (2/4 CPU, 2/4Gi memory, 20 pods, 10 services, 10 PVCs). cosign verify command for digest updates documented inline in kustomization.yaml. `kustomize build config/overlays/prod` clean. **Follow-on:** populate the digest placeholder on the first CI image publish (out of session scope). | audit 2026-04-25; D4-T7 | rule 05.12 |
| ~~TD-P2-10~~ | ~~OLM upgrade test suite under `tests/e2e/olm-upgrade/`.~~ Closed 2026-05-07: six-step kuttl suite at [tests/e2e/olm-upgrade/](../../../tests/e2e/olm-upgrade/). Flow: (00) install bundle v0.0.1-demo.1 via `operator-sdk run bundle`, provision Tenant + Workspace + WorkspaceSession; (01) assert all resources Active/Ready with CSV Succeeded; (02) push v0.0.1-demo.2, locate and approve the pending InstallPlan (Manual approval exercising TD-P2-16 Subscription); (03) assert CSV phase Replacing → Succeeded and old CSV cleaned up; (04) assert session pod stays Running through operator replacement, new operator re-reconciles a Workspace.spec.description patch, terminationGracePeriodSeconds: 90 observed; (05) drain test resources + `operator-sdk cleanup keese`. `make test-e2e-olm-upgrade` target added to Makefile with kind-cluster and operator-sdk fail-fast guards. Suite not auto-run — requires pre-loaded bundle images (documented in `tests/e2e/olm-upgrade/README.md`). All 6 YAML files SPDX-headed. No `sleep` calls — all polling via `kubectl wait`. | audit 2026-04-25 | [docs/designs/14a-olm-channels-upgrades.md](../../designs/14a-olm-channels-upgrades.md) |
| ~~TD-P2-11~~ | ~~Release tooling scripts: `scripts/set-csv-replaces.sh`, `scripts/bundle-sign-verify.sh`, `scripts/check-bundle-drift.sh`, `scripts/check-dep-versions.sh`, `scripts/check-optional-deps.sh`, `olm-catalog-publish.yaml` workflow.~~ Closed 2026-05-07: four new scripts (note: `bundle-sign-verify.sh` was already shipped in TD-P1-05): (1) [scripts/set-csv-replaces.sh](../../../scripts/set-csv-replaces.sh) — reads `bundle/.previous-csv`, patches `spec.replaces` via `yq`, advances the pointer, appends channel+bundle stanza to FBC index template if present (TD-P3-03 forward-compat); (2) [scripts/check-bundle-drift.sh](../../../scripts/check-bundle-drift.sh) — re-runs `make bundle` then `git diff --exit-code bundle/manifests/`, `--skip-regen` flag for CI; (3) [scripts/check-dep-versions.sh](../../../scripts/check-dep-versions.sh) — validates four required direct deps against 14b version ranges via `sort -t.`; (4) [scripts/check-optional-deps.sh](../../../scripts/check-optional-deps.sh) — checks `bundle/metadata/dependencies.yaml` for all four OLM GVK deps + warns on absent `relatedImages`. [.github/workflows/olm-catalog-publish.yaml](../../../.github/workflows/olm-catalog-publish.yaml): tag-triggered, regenerate bundle → build+push → cosign sign → `bundle-sign-verify.sh` (required check) → `opm render` into catalog → push+sign catalog; identity regexp anchored to rule 05.12. Starter `bundle/.previous-csv` committed (placeholder). All 4 scripts executable, shellcheck-clean at warning severity. | audit 2026-04-25 | [docs/designs/14a-olm-channels-upgrades.md](../../designs/14a-olm-channels-upgrades.md), [14b](../../designs/14b-olm-dependencies.md) |
| ~~TD-P2-12~~ | ~~Memory backends beyond sqlite: redis, qdrant, pgvector, neo4j, mem0, zep. All BackendProvisioner stubs.~~ Closed 2026-05-07: six `memory_<backend>_backend.go` files implement `BackendProvisioner`; `MultiBackendProvisioner` (`memory_multi_backend.go`) dispatches all 7 provider types. `SetupWithManager` wires `NewMultiBackendProvisioner`. **redis**: external (address set) no-op; in-cluster SSA `apps/v1.StatefulSet` + headless Service (`redis:7`). **qdrant**: external (endpoint set) no-op; tries `qdrant.io/v1.QdrantCluster` unstructured SSA, falls back to StatefulSet (`qdrant/qdrant:v1.13.0`). **pgvector**: external (dsnSecretRef set) Secret-exists probe; in-cluster tries `postgresql.cnpg.io/v1.Cluster` unstructured SSA with pgvector init SQL, falls back to StatefulSet (`pgvector/pgvector:pg17`). **neo4j**: external (URI set) no-op; in-cluster StatefulSet (`neo4j:5-community`). **mem0**: SSA `external-secrets.io/v1.ExternalSecret` (unstructured) to bridge credential from OpenBao; no-op if ESO CRD absent. **zep**: cloud mode (apiEndpoint set) ExternalSecret; self-hosted StatefulSet with credential projected at `/var/run/keese/secrets/zep` (rule 05.7). All operator CRD types use `unstructured.Unstructured` — no go.mod churn. `go build ./...` + `go test -short -race ./internal/controller/keese/...` green; 50+ unit tests in `memory_backends_test.go`. | D2-T2 | [docs/designs/15-memory-management.md](../../designs/15-memory-management.md) |
| ~~TD-P2-13~~ | ~~Additional LLM providers: AWS Bedrock (OIDC-STS), GCP Vertex (WIF), Azure OpenAI (Entra), credential pooling.~~ Closed 2026-05-07: three provider stacks under [dev/bootstrap/aigateway/](../../../dev/bootstrap/aigateway/): (1) [aws-bedrock-stack.yaml](../../../dev/bootstrap/aigateway/aws-bedrock-stack.yaml) — `ExternalSecret` (role ARN + region from OpenBao), `Backend` (`bedrock-runtime.us-east-1.amazonaws.com:443`), `BackendTLSPolicy`, `BackendSecurityPolicy` `type=AWSCredentials` (OIDC-STS; no static key), `AIServiceBackend` `schema=AWS`, `AIGatewayRoute` matching Amazon Nova/Titan model IDs. (2) [gcp-vertex-stack.yaml](../../../dev/bootstrap/aigateway/gcp-vertex-stack.yaml) — `ExternalSecret` (WIF pool/provider/SA email from OpenBao), `Backend` (`us-central1-aiplatform.googleapis.com:443`), `BackendTLSPolicy`, `BackendSecurityPolicy` `type=GoogleCredentials` (WIF token exchange; no SA key), `AIServiceBackend` `schema=GeminiVertexAI`, `AIGatewayRoute` matching Gemini model IDs. (3) [azure-openai-stack.yaml](../../../dev/bootstrap/aigateway/azure-openai-stack.yaml) — `ExternalSecret` (tenantId + clientId from OpenBao), `Backend` (`<resource>.openai.azure.com:443`), `BackendTLSPolicy`, `BackendSecurityPolicy` `type=AzureCredentials` (Entra federated credential; no client secret), `AIServiceBackend` `schema=AzureOpenAI`, `AIGatewayRoute` matching GPT-4o model IDs. All three stacks: `kubectl --dry-run=client apply` clean. Added to [kustomization.yaml](../../../dev/bootstrap/aigateway/kustomization.yaml) as commented-out resources (default off; Anthropic is the demo path). Credential pooling deferred — requires design 17 state machine (still stub). | D3-T2 | [docs/designs/05b-credential-injection-patterns.md](../../designs/05b-credential-injection-patterns.md) |
| ~~TD-P2-14~~ | ~~TokenBudget enforcement on session pods...~~ Closed 2026-05-07: [internal/controller/keese/workspacesession_controller.go](../../../internal/controller/keese/workspacesession_controller.go) `Reconcile` now calls `r.checkTokenBudget(ctx, &sess, &ws)` before pod provisioning. Reads `TokenBudget.status.consumed` (written by the policy/tokenbudget controller — rule 04.4 cross-controller status reads are fine) and compares against `spec.limit`. Two new condition types (`TokenBudgetWithinLimit`, `TokenBudgetExceeded`) plus the `ReasonTokenBudgetExceeded` event. Unlimited-default behavior: if no TokenBudget CR is set for the tenant, sessions are NOT gated (documented inline). RBAC marker added: `+kubebuilder:rbac:groups=policy.keese.ai,resources=tokenbudgets,verbs=get;list;watch`. | audit 2026-04-25 | [docs/specs/policy.keese.ai-v1alpha1.md](../../specs/) |
| ~~TD-P2-15~~ | ~~Projected SA token audiences `workflowRun` and `supervisor`...~~ Closed 2026-05-07: [internal/controller/keese/workspacesession_controller.go](../../../internal/controller/keese/workspacesession_controller.go) `sessionPodVolumes` now projects three SA token sources at `/var/run/keese/tokens/{egress,workflowRun,supervisor}` per design 04b iter-3. Egress is unconditional (`keese-egress-<tenant>`); workflowRun (`keese-wf-<wfrun-uid>`) is conditional on annotation `keese.ai/workflowrun-uid` set by the Workflow controller; supervisor (`keese-supervisor-<workspace-uid>`) is conditional on annotation `keese.ai/supervisor-uid` set by the operator when `Workspace.spec.supervisorRef` is configured. Token-path env vars (`KEESE_EGRESS_TOKEN_PATH`, `KEESE_WORKFLOWRUN_TOKEN_PATH`, `KEESE_SUPERVISOR_TOKEN_PATH`) match the projection — published only when the corresponding source is in the volume. TTL stays at 600s (rule 05.3). | D2-T4 | [docs/designs/04b-projected-sa-identity.md](../../designs/04b-projected-sa-identity.md) |
| ~~TD-P2-16~~ | ~~Operator + bundle Subscription manifest declaring `installPlanApproval: Manual` for the `stable` channel.~~ Closed 2026-05-07: three Subscription manifests committed at [config/subscription/](../../../config/subscription/). `keese-stable-subscription.yaml` — `installPlanApproval: Manual`, channel `stable`; `keese-candidate-subscription.yaml` — `installPlanApproval: Manual`, channel `candidate` (72h bake window per design 14a §1); `keese-fast-subscription.yaml` — `installPlanApproval: Automatic`, channel `fast` (CI/dev clusters, with explicit WARNING comment against production use). `config/subscription/kustomization.yaml` lists all three; usage note documents that only one should be applied per cluster. All four files SPDX-headed. `kubectl --dry-run=client apply -f config/subscription/` clean. TD-P2-16 Subscription exercised by TD-P2-10 kuttl suite step 02 (InstallPlan approval). | upgrade audit 2026-04-25 | [docs/designs/14a-olm-channels-upgrades.md](../../designs/14a-olm-channels-upgrades.md) |
| ~~TD-P2-17~~ | ~~Liveness probe values must align with terminationGracePeriodSeconds: 60...~~ Closed 2026-05-06: bumped grace to 90s in [config/manager/manager.yaml](../../../config/manager/manager.yaml). The drain budget (lease 5s + queue 30s + OTEL 15s + buffer 10s = 60s) at exactly 60s grace gave zero slack against the probe window (30 + 10*3 = 60s). Chose grace bump over probe relaxation because slower hung-process detection is a worse operational tradeoff than a longer shutdown window. Inline comments in manager.yaml document the math so future probe edits don't reintroduce the boundary case. config/overlays/prod/kustomization.yaml carries the same 90s grace via JSON-Patch for rule-05.12 prod parity. | D1-T4 | [docs/designs/18-process-lifecycle.md](../../designs/18-process-lifecycle.md) |
| TD-P2-18 | external-secrets chart upgrade 0.10.5 → 1.x or 2.x. Upstream chart index dropped 0.x; current index starts at 1.x with breaking SecretStore CRD field-path changes. Plan migration before next chart-pin sweep. | TD-P1-08 closure 2026-05-06 | n/a |

## P3 — within one quarter

| ID | Item | Source | Spec / Design |
|---|---|---|---|
| ~~TD-P3-01~~ | ~~OpenTofu modules under `deploy/opentofu/` for EKS / GKE / AKS. Designed fully in 21; zero code.~~ Closed 2026-05-07: three self-contained modules authored under [deploy/opentofu/aws/](../../../deploy/opentofu/aws/), [deploy/opentofu/gcp/](../../../deploy/opentofu/gcp/), [deploy/opentofu/azure/](../../../deploy/opentofu/azure/) — each ships `main.tf`, `variables.tf`, `outputs.tf`, `versions.tf`, `README.md`. AWS: EKS managed node groups (≥2 AZ), IAM OIDC provider, per-cluster IRSA role, KMS secrets encryption. GCP: GKE Autopilot (regional, private nodes), Workload Identity binding, CMEK database encryption. Azure: AKS (`Standard_D4s_v5`, ≥2 AZ), Entra user-assigned managed identity + federated credential, CMK disk encryption. State backend configs documented as placeholder patterns (no real bucket names). `tofu fmt -check -recursive deploy/opentofu/` returns 0. Index at [deploy/opentofu/README.md](../../../deploy/opentofu/README.md). Paired with TD-P3-06. | audit 2026-04-25; D4-T1 | [docs/designs/21-opentofu-cloud-deployment.md](../../designs/21-opentofu-cloud-deployment.md) |
| TD-P3-02 | Conversion webhooks for CRD `v1alpha1 → v1beta1` promotions. Rule 04.13 explicitly defers to first promotion; track when first kind hits beta. | rule 04.13 | rule 04.13 |
| ~~TD-P3-03~~ | ~~OperatorHub catalog publishing path: `ghcr.io/keese-ai/keese-catalog`. CSV `replaces:` chain automation.~~ Closed 2026-05-07: (1) [bundle/.config/index-template.yaml](../../../bundle/.config/index-template.yaml) — FBC template for `keese-catalog` with keese package + cert-manager, Capsule, Argo Workflows, ExternalSecrets dep entries per 14b §dependencies. (2) [scripts/build-catalog.sh](../../../scripts/build-catalog.sh) — renders via `opm alpha render-template basic` → `catalog/keese/catalog.json` and validates with `opm validate`. (3) [Dockerfile.keese-catalog](../../../Dockerfile.keese-catalog) — minimal FBC image on `quay.io/operator-framework/opm:latest`, embeds `catalog/` and validates at build time. (4) [.github/workflows/catalog-publish.yaml](../../../.github/workflows/catalog-publish.yaml) — tag-push workflow: renders template, validates, builds + pushes catalog image, cosign keyless signs + self-verifies (rule 05.12 identity regex), attaches syft SBOM via `cosign attest`. (5) [scripts/set-csv-replaces.sh](../../../scripts/set-csv-replaces.sh) extended with step 04 (`step_update_catalog_index`) that uses a Python regex pass to insert a new `replaces:`-aware channel entry + bundle stanza into the FBC template on each release; `python3` added to `_check_deps`. `go build ./...` clean. | upgrade audit | [docs/designs/14b-olm-dependencies.md](../../designs/14b-olm-dependencies.md) |
| ~~TD-P3-04~~ | ~~InjectPrompt SPI method (design 23 iter-2 flagged) — bumps the AgentRuntime SPI.~~ Closed 2026-05-07: `SupportsInjectPrompt` flipped to `true` in capabilities matrix. Real implementation in [internal/runtime/providers/goose/goose.go](../../../internal/runtime/providers/goose/goose.go): shells into the session pod via `PodExecutor` and writes the sanitised prompt to the named FIFO at `/var/run/keese/session/home/.local/state/goose/inject-fifo` (goose XDG state home = `/var/run/keese/session/home`). Shell script creates the FIFO idempotently, writes via a backgrounded sub-shell with 5 s kill guard (no hang if goose not listening). Embedded newlines collapsed before write (one write = one supervisor turn, design 23 §step 2). `shellescape` helper prevents shell injection from single-quote payloads. **Fifo backing:** no upstream goose CLI doc found for this exact path — path is keese-conventional, consistent with `$HOME/.local/state/goose/` XDG state layout; actual goose fifo support requires TD-P3-05 fork to add the reader. 4 new unit tests: `TestInjectPromptWritesToFifo` (table-driven: simple/newlines/single-quotes), `TestInjectPromptNoPodName`, `TestInjectPromptNoExecutor`, `TestInjectPromptCapabilityEnabled`. `go test -short -race ./internal/runtime/...` green. | upgrade audit | [docs/designs/23-agent-supervision.md](../../designs/23-agent-supervision.md) |
| TD-P3-05 | Real keese fork of goose (`internal/runtime/providers/goose/`). Demo uses `ghcr.io/block/goose:latest` upstream image directly. | DD-4 | [docs/designs/08a-goose-headless-modes.md](../../designs/08a-goose-headless-modes.md) |
| ~~TD-P3-06~~ | ~~OPA / Conftest Rego policies for OpenTofu cloud deployments.~~ Closed 2026-05-07: four Rego files in package `keese.opentofu.security` under [policy/opentofu/](../../../policy/opentofu/): `deny-public-cluster.rego` (EKS public endpoint without CIDR lock-down, GKE public master without authorized networks, AKS unrestricted API IP ranges), `deny-default-iam.rego` (EKS node groups using default/shared IAM role ARN patterns or missing role), `require-encryption-at-rest.rego` (EKS missing `encryption_config[secrets]`, GKE `database_encryption.state != ENCRYPTED`, AKS missing `disk_encryption_set_id`), `deny-public-bucket.rego` (S3 public ACLs + incomplete public access block, GCS `allUsers`/`allAuthenticatedUsers` IAM members, Azure `allow_blob_public_access=true`). Each `.rego` ships a sibling `_test.rego` with table-driven allow + deny cases; `conftest verify --policy policy/opentofu/` runs **29 tests, 29 passed**. `make tofu-validate` wires conftest against `policy/opentofu/`. Paired with TD-P3-01. | audit 2026-04-25 | [docs/designs/21-opentofu-cloud-deployment.md](../../designs/21-opentofu-cloud-deployment.md) |
| ~~TD-P3-07~~ | ~~Multi-tenant + cross-tenant smoke tests. Demo is single-tenant.~~ Closed 2026-05-07: kuttl suite at [tests/e2e/multi-tenant/](../../../tests/e2e/multi-tenant/) (4 files, 4 test steps). Provisions two concurrent Tenants (`alpha`, `beta`) each with a Workspace + WorkspaceSession; asserts both reach Active/Ready in parallel; then launches a busybox probe pod in the `alpha` namespace toward the `beta-memory` Service ClusterIP and asserts it exits `Failed` (NetworkPolicy blocks cross-tenant egress per rule 04.17 + rule 05.4-5). CrossTenantAgreement happy path deferred — requires a live OpenFGA instance for tuple negotiation; documented in suite README. New Makefile target `make test-e2e-extended` runs all four suites (workspace-progression + agentruntime-drain + multi-tenant + chaos-network) against an existing kind cluster with a fail-fast guard if the cluster is absent. | D5 §verification | [docs/specs/keese.ai-v1alpha1.md](../../specs/) |
| ~~TD-P3-08~~ | ~~Chaos / network-partition / disk-full tests. None today.~~ Closed 2026-05-07: kuttl suite at [tests/e2e/chaos-network/](../../../tests/e2e/chaos-network/) (8 files, 8 test steps). Flow: (1) provision Workspace + WorkspaceSession in `chaos-test` namespace; (2) assert healthy baseline; (3) inject deny-all egress NetworkPolicy — asserts `EgressUnavailable=True` condition within 30s and a netcat probe exits non-zero (no hang); (4) delete the fault policy — asserts `EgressUnavailable=False` and egress recovers within 30s; (5) `kubectl rollout restart` the keese-controller-manager — asserts session pod survives (not deleted), session reconverges to `Active`, and manager reaches `Available` within 60s. Disk-full scenario deferred — requires kubelet storage eviction configuration beyond kuttl scope; tracked as a follow-on. `make test-e2e-extended` includes this suite. | D5 §verification | n/a |
| ~~TD-P3-09~~ | ~~Backup + DR runbook for OpenBao + OpenFGA + NATS streams.~~ Closed 2026-05-07: four docs authored under `docs/references/`: index at [backup-and-dr.md](../../references/backup-and-dr.md) (summary matrix + guiding principles) plus component runbooks [backup-and-dr-openbao.md](../../references/backup-and-dr-openbao.md) (daily CSI VolumeSnapshot, 3-of-5 unseal, cloud KMS auto-unseal rotation runbook), [backup-and-dr-openfga.md](../../references/backup-and-dr-openfga.md) (`fga store export/import`, ConfigMap mirror update, smoke verification), [backup-and-dr-nats.md](../../references/backup-and-dr-nats.md) (`nats stream backup/restore`, per-stream cadence trade-offs, consumer sequence reset). Each file ≤ 200 lines. References README and CLAUDE.md task table updated. | D4 out-of-scope | [docs/references/backup-and-dr.md](../../references/backup-and-dr.md) |
| TD-P3-10 | OPA Scorecard hardening past current state. | rule 02 supply chain | rule 02 |

## Tracking

- Open one GitHub issue per row when work begins, link the issue ID in
  this table.
- Update `last_verified` on this doc whenever a row moves status
  (don't let it rot post-demo).
- When a P1 ships, move its row to a "Closed" section at the bottom or
  delete it; don't keep historical rows inline.

## Refinement passes

This is a register, not a phase plan. No iteration log. Score-against-rubric
applies only to the individual fix plans authored when each row's work
starts.

## See also

- [README.md](README.md) — demo phase index.
- [docs/plans/gate-open-audit-2026-04-22.md](../gate-open-audit-2026-04-22.md) — the original honest-score audit; this register extends it post-demo.
- [MEMORY.md](../../../MEMORY.md) — running gotchas; cross-link big fixes.
