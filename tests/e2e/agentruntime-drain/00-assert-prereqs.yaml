# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 keese-ai
#
# kuttl step 00: assert that the required Tenant and Workspace objects exist
# and are in a ready state before starting the drain test.
#
# This step is an assertion-only step (no apply) — it verifies the cluster
# was bootstrapped by the preceding kuttl suite steps.
apiVersion: workspace.operator.keese.ai/v1alpha1
kind: Workspace
metadata:
  name: drain-test-ws
  namespace: drain-test
status:
  phase: Active
