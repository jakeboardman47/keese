---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: recipesources.keese.ai
spec:
  group: keese.ai
  names:
    kind: RecipeSource
    listKind: RecipeSourceList
    plural: recipesources
    singular: recipesource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.sourceType
      name: Type
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RecipeSource is the Schema for the recipesources API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RecipeSourceSpec defines the desired state of RecipeSource.

              Exactly one of oci, git, or configMap must be set (discriminated one-of).
            properties:
              configMap:
                description: ConfigMap specifies an inline ConfigMap source (dev namespaces
                  only).
                properties:
                  name:
                    description: Name is the ConfigMap name.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the ConfigMap namespace.
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              git:
                description: Git specifies a git repository source with a pinned commit
                  SHA.
                properties:
                  revision:
                    description: Revision is the full 40-character commit SHA.
                    pattern: ^[0-9a-f]{40}$
                    type: string
                  secretRef:
                    description: SecretRef references a Secret containing git credentials.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  url:
                    description: URL is the git repository URL.
                    minLength: 1
                    type: string
                required:
                - revision
                - url
                type: object
              oci:
                description: OCI specifies an OCI artifact source (preferred in production).
                properties:
                  digest:
                    description: |-
                      Digest is the immutable content-addressable digest, e.g. "sha256:abc123...".
                      Required in non-dev namespaces (VAP rule).
                    type: string
                  registry:
                    description: Registry is the OCI registry hostname, e.g. "ghcr.io".
                    minLength: 1
                    type: string
                  repository:
                    description: Repository is the image repository path.
                    minLength: 1
                    type: string
                  secretRef:
                    description: |-
                      SecretRef references a Secret containing OCI pull credentials.
                      The secret is mounted as a projected file (rule 05.7).
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  tag:
                    description: Tag is the image tag (dev only; digest takes precedence).
                    type: string
                required:
                - registry
                - repository
                type: object
            type: object
            x-kubernetes-validations:
            - message: exactly one of oci, git, or configMap must be set
              rule: '(has(self.oci) ? 1 : 0) + (has(self.git) ? 1 : 0) + (has(self.configMap)
                ? 1 : 0) == 1'
          status:
            description: RecipeSourceStatus defines the observed state of RecipeSource.
            properties:
              cached:
                description: Cached is true when the artifact has been written to
                  the cluster-internal cache.
                type: boolean
              conditions:
                description: Conditions contains detailed status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastVerifiedTime:
                description: LastVerifiedTime is the timestamp of the most recent
                  successful cosign verification.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Synced
                - Failed
                type: string
              resolvedDigest:
                description: |-
                  ResolvedDigest is the content-addressable digest of the cached artifact.
                  Written after cosign verification succeeds.
                type: string
              sourceType:
                description: SourceType records which source type is active.
                enum:
                - OCI
                - Git
                - ConfigMap
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
